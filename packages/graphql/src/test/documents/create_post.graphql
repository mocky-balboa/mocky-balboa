mutation create_post($input: CreatePostInput!) {
	createPost(input: $input) {
		id
		title
		content
		excerpt
		slug
		author {
			id
			username
		}
		category
		tags
		publishedAt
		createdAt
		updatedAt
		likes
		views
		isPublished
		featuredImage
		readingTime
	}
}
