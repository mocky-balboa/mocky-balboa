---
sidebar_position: 20
title: Nuxt
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Mocky Balboa and Nuxt

How to use Mocky Balboa with [Nuxt](https://nuxtjs.org).

:::info[Supported versions]

__≥ 3.0.0__

:::

## Installation

<Tabs groupId="package-manager">
  <TabItem value="pnpm" label="pnpm" default>
      ```bash
      pnpm add -D @mocky-balboa/nuxt
      ```
    </TabItem>
  <TabItem value="npm" label="npm">
    ```bash
    npm install -D @mocky-balboa/nuxt
    ```
  </TabItem>
  <TabItem value="yarn" label="yarn">
    ```bash
    yarn add -D @mocky-balboa/nuxt
    ```
  </TabItem>
</Tabs>

## Usage

Everything needed to setup the integration is included in the `@mocky-balboa/nuxt` module.

<Tabs groupId="javascript-language">
  <TabItem value="typescript" label="TypeScript" default>
        ```TypeScript
        // nuxt.config.ts

        export default defineNuxtConfig({
          // Enabled defaults to true
          modules: [["@mocky-balboa/nuxt", { enabled: true }]],
        });
        ```
  </TabItem>
  <TabItem value="javascript" label="JavaScript">
      ```JavaScript
      // nuxt.config.js

      export default defineNuxtConfig({
        // Enabled defaults to true
        modules: [["@mocky-balboa/nuxt", { enabled: true }]],
      });
      ```
  </TabItem>
</Tabs>

Make sure `enabled` is set to `false` for your production like environments. You only want to enable the module when running your tests.

Now run your Nuxt application as you normally would, either via build and server or dev.

[Module options API reference](https://api-reference.mockybalboa.com/interfaces/_mocky-balboa_nuxt..MockyBalboaModuleOptions)
